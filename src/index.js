import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
// component는 html을 return하는 함수
// JSX = HTML + JavaScript
// react application은 하나의 component만을 렌더링해야한다.